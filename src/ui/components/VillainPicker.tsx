// The active villain-picker screen lives in `app/routes.tsx` (`VillainPicker`).
// This file is reserved for the standalone, reusable picker the UI will host
// in CHUNK 6+ when multiple villains are selectable.

export {};
