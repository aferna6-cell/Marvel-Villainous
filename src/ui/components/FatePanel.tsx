// Fate-prompt resolver UI lands in a later chunk when full Fate mechanics ship
// to the UI. The placeholder keeps the file tracked and the import shape stable.

export function FatePanel(): JSX.Element | null {
  return null;
}
